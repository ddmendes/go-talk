version: '3.3'
services:
  db:
    build: .
    environment:
      POSTGRES_DB: ghdata
      POSTGRES_USER: root
      POSTGRES_PASSWORD: toor
    ports:
      - 5432:5432
    volumes:
      - postgres-data:/var/lib/postgres/data

volumes:
  postgres-data:
